javascript-htmlspecialchars
===========================

handle view special chars in HTML

if string ```&#lt;```
show in HTML ```<```

use javascript-htmlspecialchars
string ```&#lt;```
output in HTML ```&#lt;```
